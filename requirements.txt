# JetSnack UI automation (Python 3.10+; 3.12 tested).
# Appium 2 server + UiAutomator2 driver on the host; see doc/SETUP.md.
# Install: python -m pip install -r requirements.txt

Appium-Python-Client>=3.1.0,<6.0.0
pytest>=8.0.0,<10.0.0
